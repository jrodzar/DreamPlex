# -*- coding: utf-8 -*-
"""Stub of enigma2's Plugins.Plugin."""


class PluginDescriptor(object):
	WHERE_PLUGINMENU = 1
	WHERE_AUTOSTART = 2
	WHERE_SESSIONSTART = 3
	WHERE_MENU = 4
	WHERE_EXTENSIONSMENU = 5

	def __init__(self, *a, **k):
		pass
